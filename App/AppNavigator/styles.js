// @flow

import { StyleSheet } from 'react-native'

import { APP_BACKGROUND } from '../styles'

const styles = StyleSheet.create({
    card: {
        backgroundColor: 'transparent'
    },
    screen: {
        flex: 1,
        backgroundColor: APP_BACKGROUND
    }
})

export default styles
